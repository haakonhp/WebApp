export type Tournament = {
    id: string,
    description: string,
    goal: string,
    name: string,
    priority: number,
    userId: string,
    sport: string,
    createdAt: Date,
    date: Date,
    location: string
}